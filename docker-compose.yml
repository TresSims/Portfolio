version: '3'
services:
	backend:
		image: strapi/strapi
		volumes:
			- ./app:/srv/app
		ports:
			- 1337:1337
		restart: unless-stopped
	frontend:
		build: frontend
		volumes:
			- /code/node_modules
			- ./frontend:/code
		ports:
			- 8801:3000
		restart: unless-stopped